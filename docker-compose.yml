resource-viewer:
  build: .
  environment:
    NODE_ENV: development
  command: server --watcher polling
  ports:
    - "4200:4200"
  volumes:
    - .:/resource-viewer
    - /resource-viewer/node_modules
